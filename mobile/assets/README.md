# App Icons & Splash

Add your `icon.png` (1024x1024), `adaptive-icon.png` (1024x1024, foreground only),
and `splash.png` here, then reference them from `app.json`'s `expo.icon`,
`expo.android.adaptiveIcon.foregroundImage`, and `expo.splash` fields.
